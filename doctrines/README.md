# Doctrines

This directory houses declarative doctrine specifications used by the NeuroSymbolic Core. Source markdown doctrine briefs in `docs/` are distilled into YAML definitions that the compiler ingests.
